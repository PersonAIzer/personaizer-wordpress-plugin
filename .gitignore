# Built plugin zips + manifests land here — consistent, predictable location, but never committed.
# A committed zip goes stale and you end up testing the wrong version (build-zip.sh regenerates it).
dist/
